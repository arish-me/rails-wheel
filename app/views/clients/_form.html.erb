<%= styled_form_with model: client, class: "space-y-4" do |f| %>
  <% if client.errors.any? %>
    <%= render "shared/form_errors" , model: client %>
  <% end %>
  <section class="space-y-2 overflow-hidden">
    <%= f.text_field :name, label: 'Name' , placeholder: 'Enter Name' %>
    <%= f.text_field :subdomain, label: 'Subtitle' , placeholder: 'Enter Subdomain' %>

  </section>
  <section>
    <%= f.submit 'Submit' %>
  </section>
<% end %>