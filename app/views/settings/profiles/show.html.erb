<%= content_for :page_title, "Profile" %>
<%= settings_section title: "Profile", subtitle: "Customize how you appear on Wheel" do %>
  <%= styled_form_with(model: @user, url: registration_path(@user), method: :patch, class: "space-y-4", data: { controller: "searchable-select" }) do |form| %>
    <%= render "settings/user_avatar_field", form: form, user: @user %>
  <div>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mt-4">
      <%= form.text_field :first_name, placeholder: "First name", label: "First Name" %>
      <%= form.text_field :last_name, placeholder: "Last name", label: "Last Name" %>
    </div>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mt-4">
      <div class="w-full">
        <%= form.select :gender,  User.genders.map { |key, _| [User::GENDER_DISPLAY[key.to_sym], key] }, { include_blank: "---Select---", label: "Pronounce" } %>
      </div>
      <div class="w-full">
        <%= form.telephone_field :phone_number, placeholder: "+1 (555) 123-4567" , label: "Phone" %>
      </div>

      <div class="w-full">
        <%= form.date_field :date_of_birth,   max: Date.today - 10.years, min: Date.today - 120.years, label: "Date of birth" %>
      </div>
      <% if false %>
      <div class="sm:col-span-2">
        <%= form.label :bio, class: 'block mb-2 text-sm font-medium text-gray-900 theme-dark:text-white' %>
        <%= form.text_area :bio, rows: 4, class: "block p-2.5 w-full text-sm text-gray-900 bg-gray-50 rounded-lg border border-gray-300 focus:ring-blue-500 focus:border-blue-500 theme-dark:bg-gray-700 theme-dark:border-gray-600 theme-dark:placeholder-gray-400 theme-dark:text-white theme-dark:focus:ring-blue-500 theme-dark:focus:border-blue-500", placeholder: "Write a short bio about yourself..." %>
        <p class="mt-1 text-xs text-gray-500 theme-dark:text-gray-400">Maximum 500 characters</p>
      </div>
      <% end %>
    </div>
  </div>

  <div class="flex justify-end mt-4">
    <%= render ButtonComponent.new(text: t(".save"), class: "md:w-auto w-full justify-center") %>
  </div>
<% end %>

<% end %>